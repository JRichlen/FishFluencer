# === FishFluencer Schedules ===

# Social media posting times (24h format)
post_times:
  - "09:00"
  - "17:00"

# Image purge interval
purge_interval_hours: 1

# GitHub sync interval (minutes)
sync_interval_minutes: 15

# Temperature read interval (minutes)
temp_read_interval_minutes: 5

# Snapshot interval (minutes)
snapshot_interval_minutes: 15
